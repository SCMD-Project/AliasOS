// AliasOS delayed boot presentation.  aos_redraw is the exported redraw
// entry; internal __aos_tty_* names are not stable aliases in the compiled
// output, so cfg scripts must not call them directly.
sleep 32
showconsole
sleep 32
clear
sleep 32
aos_redraw
